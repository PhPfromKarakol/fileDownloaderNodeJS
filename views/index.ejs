<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
</head>

<body>
    <div class="">
        <%- include('partial/menu')%>
    </div>
    <div class="">
        <h1>
            <%= descip %>
        </h1>
    </div>

    <div class="">
        <ul>
            <% for(var i=0 ; i < links.length ; i++) { %>
                <li>
                    <a href="/uploads/<%= links[i] %>" download="">
                        <%= links[i] %>
                    </a>
                </li>
                <% } %>
        </ul>
    </div>

</body>

</html>
